# Policies

The actual policy documents live in [`/docs`](../../docs/README.md), not
here — this folder exists so an auditor browsing `/evidence` finds a
pointer rather than a dead end. Policies aren't evidence themselves;
they're what the rest of `/evidence` proves was actually followed.
